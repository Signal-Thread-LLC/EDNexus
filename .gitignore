# Build output
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/

# .NET / Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates
*.rsuser
project.lock.json
project.fragment.lock.json
artifacts/

# Rider / VS Code
.idea/
.vscode/

# Test / coverage
[Tt]est[Rr]esult*/
*.coverage
*.coveragexml
coverage*.json

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
!packages/build/

# OS
.DS_Store
Thumbs.db

# Local overrides
*.local.json
appsettings.*.Local.json
# Per-environment ASP.NET config: the EBS's dev config holds the Twitch client secret and
# extension secret, so it must never be committable.
appsettings.Development.json
# Container secrets. The committed .env.example is the template and holds no real values.
.env
!.env.example
appsettings.*.Development.json

# Installer build output
installer/**/publish/
installer/**/stage/
installer/**/out/
